pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'attach-extended'

include 'log'
include 'yt'

project(':log').projectDir                     = file('modules/log')
project(':yt').projectDir                      = file('modules/yt')